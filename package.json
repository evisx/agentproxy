{
  "name": "agentproxy",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Stateless transparent HTTP/HTTPS proxy for Cloudflare Workers",
  "scripts": {
    "dev": "wrangler dev",
    "build": "wrangler deploy --dry-run --outdir .wrangler-dist",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vitest": "^3.2.4",
    "wrangler": "^4.48.0"
  }
}
